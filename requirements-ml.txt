# Optional ML dependencies for src/framing_scores.py
# Install with: pip install -r requirements-ml.txt
#
# PyTorch: install the build matching your hardware FIRST, before the rest:
#   NVIDIA CUDA:  pip install torch --index-url https://download.pytorch.org/whl/cu121
#   AMD ROCm:     pip install torch --index-url https://download.pytorch.org/whl/rocm6.1
#   CPU only:     pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# Then install the rest:
sentence-transformers>=2.6
transformers>=4.40
accelerate>=0.30
scikit-learn>=1.4
scipy>=1.12
